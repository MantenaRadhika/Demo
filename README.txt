Commanly used Git Commands

mkdir <new directory name>
ls -al
cd  <new directory name>/
git init <new reprositor name>
ls
cd <new reprositor name>/
crete new test file in <new reprositor>
Use git add 
Use git commit -m "message"
Use git log
Use git show